/**
 * tailwind.config.js — NOT USED with Tailwind v4
 *
 * This project uses Tailwind CSS v4, which uses a CSS-first configuration
 * approach via @theme in src/index.css. All custom colors, animations, and
 * other design tokens are defined there.
 *
 * See: https://tailwindcss.com/docs/v4-upgrade
 */
export default {};
